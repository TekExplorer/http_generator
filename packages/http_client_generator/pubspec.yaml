name: http_client_generator
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

resolution: workspace

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  http_client_annotation:
  analyzer_buffer: ^0.1.11
  analyzer: '>=8.0.0 <9.0.0'
  source_gen: ^4.0.1
  source_helper: ^1.3.8
  build: ^4.0.1
  collection: ^1.19.1
  source_span: ^1.10.1
  # path: ^1.9.0

dev_dependencies:
  build_runner:
  build_test: ^3.4.1
  lints: ^6.0.0
  test: ^1.25.6
  http: 
